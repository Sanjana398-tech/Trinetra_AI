# Documentation Index

This folder will accumulate detailed docs as each phase is built:

- `installation.md` — expanded setup guide (added when auth/env complexity grows)
- `architecture.md` — module boundaries, data flow between detection engines
- `api.md` — JSON API reference (added once modules expose endpoints)
- `model-training.md` — how to retrain `message_model.pkl` / `url_model.pkl` (Phase 3)
- `database-schema.md` — ER diagram and column reference for `database/trinetra.db`

For now, see the root `README.md` for setup and the current phase roadmap.
