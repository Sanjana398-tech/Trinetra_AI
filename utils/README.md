# utils/

Standalone helper scripts shared across the project (e.g. dataset
cleaning scripts, one-off migration/maintenance scripts) that are NOT
part of the Flask application itself.

Application-internal utilities used by Flask routes live in
`backend/utils/` instead.

Populated starting Phase 2+.
